# portfolio
My personal portfolio with all my experiences, skills and some features.

## Context Files

Read the following to get full context of the project:

 - @context/project-overview.md
 - @context/coding-standards.md
 - @context/ai-interaction.md
 - @context/current-feature.md


## Next.js version warning

This project runs **Next.js 16.2.10**, a version released after your training cutoff. APIs, conventions, and file structure may differ from what you expect. Before writing or modifying any Next.js-related code (routing, config, data fetching, metadata, etc.), read the relevant guide under `node_modules/next/dist/docs/` (organized as `01-app/`, `02-pages/`, `03-architecture/`, `04-community/`). Do not assume prior knowledge of the App Router APIs is still accurate — verify against these local docs first.

## Commands

- `npm run dev` — start the dev server (Turbopack) at http://localhost:3000
- `npm run build` — production build
- `npm run start` — run the production build
- `npm run lint` — run ESLint (flat config via `eslint-config-next`)

There is no test framework configured in this repo yet.
